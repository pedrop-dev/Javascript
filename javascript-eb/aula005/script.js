/*
    ARRAY :
    push    (adiciona um número selecionado ao final do array)
    pop     (remove o último valor do array)
    shift   (remove o primeiro valor do array)
    unshift (adiciona um valor ao início do array)
    length (datermina o tamanho do array)
*/

const data = [1, 2, 3]

data.push(4)
//data.pop()
//data.shift()
//data.unshift()

console.log(data)